import { SUBSCRIBER } from "cache";
import { SniperEntity } from "entities";
import { parseRequest, respondTo } from "../common";
import { SniperRequests } from "./sniper.requests";

export const initializeSniperHandlers = () => {
  // Queries
  // Mutations
};
